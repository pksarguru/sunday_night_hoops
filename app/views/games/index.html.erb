<div class="page-header">
  <h1>
    Games
  </h1>
</div>

<%= render partial: "games/current_game_card", locals: { game: @current_game } %>

<% @upcoming_games.each do |game| %>
  <%= render partial: "games/game_card", locals: { game: game } %>
<% end %>
